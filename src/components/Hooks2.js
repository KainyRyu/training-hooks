import React, { useEffect }from 'react'

export default function Hooks2() {
    useEffect(() => {

    })

    return (
        <div>
            <h1>{couter}</h1>
            <button onClick={increase}></button>
        </div>
    )
}
